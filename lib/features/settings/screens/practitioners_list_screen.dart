import 'package:flutter/material.dart';

class PractitionersListScreen extends StatelessWidget {
  const PractitionersListScreen({super.key, required this.clinicId});
  final String clinicId;

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Practitioners — coming soon'));
  }
}
