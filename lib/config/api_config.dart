class ApiConfig {
  // NOTE:
  // Android emulator cannot reach laptop via localhost.
  // Use 10.0.2.2 to access the host machine's local API.
  static const String baseUrl = 'http://10.0.2.2:5000';
}
